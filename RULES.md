# قواعد المشروع الشاملة - Alghwairy Sovereign Ledger

يحدد هذا الملف القواعد والمعايير التقنية والمهنية التي يجب اتباعها بدقة في تطوير وصيانة مشروع **Alghwairy Sovereign Ledger**.

## 1. المعايير التقنية (Technical Standards)

### 1.1 لغة البرمجة: TypeScript
- **الصرامة:** يجب تفعيل `strict mode`. يمنع استخدام نوع `any` منعاً باتاً.
- **التعريفات:** يجب تعريف `Interfaces` و `Types` لكل البيانات القادمة من Supabase أو المرسلة عبر IPC.
- **التعليقات:** يجب توثيق الوظائف المعقدة باستخدام JSDoc باللغة الإنجليزية/العربية.

### 1.2 إطار العمل: React & Electron
- **المكونات:** استخدام الوظائف (Functional Components) والخطافات (Hooks).
- **إدارة الحالة:** استخدام `Context API` أو `Zustand` للمنطق المعقد.
- **التواصل:** يجب أن يتم التواصل بين العمليات (IPC) عبر قنوات آمنة ومحددة في `preload.js`.

### 1.3 قواعد البيانات: Supabase & SQL
- **الأمان:** تفعيل `Row Level Security (RLS)` لكل الجداول.
- **العمليات:** استخدام `Transactions` للعمليات المالية لضمان سلامة البيانات.
- **التدقيق:** يجب تسجيل (Log) كل عمليات التعديل والحذف في جدول مخصص للتدقيق (Audit Trail).

## 2. معايير التصميم والواجهة (UI/UX Standards)

- **الهوية:** الالتزام الكامل بالهوية المؤسسية (Navy & Gold).
- **اللغة:** الواجهة الأساسية هي العربية (RTL). يجب دعم الإنجليزية (LTR) بشكل كامل.
- **التجاوب:** يجب أن تعمل الواجهة بسلاسة داخل بيئة Electron وبأبعاد مختلفة.
- **الأداء:** استخدام `Memoization` للمكونات الثقيلة لضمان سلاسة الواجهة (60 FPS).

## 3. الامتثال والمعايير القانونية (Compliance)

- **ZATCA:** يجب أن تتوافق جميع الفواتير والعمليات الضريبية مع متطلبات هيئة الزكاة والضريبة والجمارك (المرحلة الثانية).
- **الخصوصية:** تشفير البيانات الحساسة محلياً قبل التخزين أو الرفع.

## 4. نظام سير العمل (Workflow)

- **التخطيط:** لا يتم كتابة أي كود قبل تحديث ملف `TASKS.md` أو وضع خطة تنفيذ واضحة.
- **الاختبار:** يجب إجراء اختبارات يدوية أو آلية لكل ميزة جديدة قبل دمجها.
- **التوثيق:** تحديث ملف `README.md` أو الأدلة ذات الصلة عند تغيير هيكلية المشروع.

## 5. قواعد المراجعة والدمج (Git & Review)

- **الرسائل:** يجب أن تكون رسائل Commit واضحة وتصف التغيير (مثال: `feat: add ZATCA invoice validation`).
- **التنظيف:** تشغيل `npm run lint` و `tsc` للتأكد من خلو المشروع من الأخطاء قبل الرفع.
